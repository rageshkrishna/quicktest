language: python
python:
  - 2.7
install: true
script:
  - which phantomjs
  - git config user.email
  - git config user.name
  - mkdir -p shippable/outputs
  - echo "Test" > shippable/outputs/foo.txt
archive: true
